import { palette, spacing, radius, typography, shadows, layout } from './tokens';

export const theme = {
  palette,
  spacing,
  radius,
  typography,
  shadows,
  layout,
};

export type Theme = typeof theme;
